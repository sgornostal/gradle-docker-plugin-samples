group 'org.sgornostal'
version '1.0-SNAPSHOT'

subprojects {

    buildscript {
        repositories {
            jcenter()
        }
        dependencies {
            classpath 'org.sgornostal:gradle-docker-plugin:1.5.1'
        }
    }
}

